package com.mayur.summarizer

object Summarizer {
  def summarizeUrl(url: String): String = {
    // Dummy summarization logic (you can replace this with actual summarization logic)
    s"Summarized content of the URL: $url"
  }
}
